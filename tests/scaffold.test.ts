import { describe, expect, it } from 'vitest';

// Placeholder so `npm test` has a suite to run before Phase 1 adds the real ones.
describe('scaffold', () => {
  it('is set up', () => {
    expect(1 + 1).toBe(2);
  });
});
